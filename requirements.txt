# Core Django framework
Django>=4.2,<5.0

# PostgreSQL adapter (use psycopg2-binary for development)
psycopg2-binary>=2.9.9

# For production, replace psycopg2-binary with psycopg2
# psycopg2>=2.9.9

# Optional: Environment variable management
python-dotenv>=1.0.1

# Optional: Django extensions (helpful for dev tools, shell_plus, etc.)
django-extensions>=3.2.3

# Optional: Date/time utilities
pytz
